# 工作流备份

本目录是 `.github/workflows/` 的**备份副本**，非 CI 读取源
（GitHub Actions 只读 `.github/workflows/`，本目录仅作存档与参考）。

- 源：`.github/workflows/*.yml`
- 备份：本目录
- setup action 与环境配置见 `src/setup/`

## 维护

改了源 workflow 后，**人工**把对应文件复制到本目录，保持备份与源一致。
